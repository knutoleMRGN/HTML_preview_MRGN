
  # HTML_Preview_MRGN

  This is a code bundle for HTML_Preview_MRGN. The original project is available at https://www.figma.com/design/M0sMbkAskATRKw1kia7CXR/HTML_Preview_MRGN.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  